version: "3.9"


services:
  budget_api:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./budget_api/:/usr/src/budget_app/budget_api/
    ports:
      - 8000:8000
    env_file:
      - ./.env
#    depends_on:
#      - db
